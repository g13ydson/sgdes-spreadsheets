<main>
  <div class="container py-4">
    <header class="pb-3 mb-4 border-bottom">
      <a href="/" class="d-flex align-items-center text-dark text-decoration-none">
        <%= image_tag('logo.png', size: '40') %>
        <span class="fs-4">Planilhas</span>
      </a>
    </header>
    <div class="p-5 mb-4 bg-light rounded-3">
      <div class="container-fluid py-5">
        <h1 class="display-5 fw-bold">Sistema gerador de planilhas VIP635, MCI460</h1>
        <p class="col-md-8 fs-4">Resultado do processamento do arquivo de retorno</p>
        <span>O resultado é gerado com base no layout. Em caso de dúvidas contatar o banco.</span>
      </div>
    </div>
    <div class="row align-items-md-stretch">
      <div class="col-md-12">
        <%= link_to 'Voltar', root_path, class: 'btn btn-primary mb-3' %>
        <div class="h-100 p-5 bg-light border rounded-3">
          <table class="table">
            <thead>
              <tr>
                <th scope="col">Resultado para: <%= @return_result[:header_info] %></th>
              </tr>
            </thead>
            <tbody>
              <% @return_result[:error_msgs].each do |msg| %>
                <tr>
                  <th scope="row"><%= msg %></th>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
    <footer class="pt-3 mt-4 text-muted border-top">
      GTECI &copy; <%= Date.current.year %>
    </footer>
  </div>
</main>
